# PG Atlas Backend — environment variable template.
# Copy this file to .env and fill in values for local development.
# Never commit .env to version control.

# Required: canonical URL of this API instance.
# Used as the OIDC audience when verifying GitHub OIDC tokens from the SBOM action.
# For local development, use http://localhost:8000.
# For production, use the public URL (e.g. https://api.pg-atlas.example).
PG_ATLAS_API_URL=http://localhost:8000

# Optional: async SQLAlchemy database URL.
# Not required until A2 (PostgreSQL schema). Leave empty to start the API
# without a database (health and ingestion endpoints still function).
# PG_ATLAS_DATABASE_URL=postgresql+asyncpg://pg_atlas:secret@localhost:5432/pg_atlas

# Optional: log level. Defaults to INFO.
# PG_ATLAS_LOG_LEVEL=DEBUG

# Optional: how long to cache GitHub's JWKS response in memory (seconds).
# Defaults to 3600 (1 hour). Reduce during development if testing key rotation.
# PG_ATLAS_JWKS_CACHE_TTL_SECONDS=3600
